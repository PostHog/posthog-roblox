--!strict
-- Single source of truth for the SDK version and library name.
-- Keep `SdkVersion` in sync with the `version` field in wally.toml.

return {
    SdkVersion = "0.1.5",
    LibraryName = "posthog-roblox",
}
